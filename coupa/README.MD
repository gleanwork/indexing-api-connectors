This will run Coupa APIs and saves data in staging CSV files and then index

Steps to run:

1. run init.sh
2. activate python env
3. run install.sh
4. create .env or copy _.env-example and enter all credentials and configs
5. execute run.sh
